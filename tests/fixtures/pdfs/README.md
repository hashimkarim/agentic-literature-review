Put local PDF fixtures for backend import tests anywhere under this directory.

The test suite recursively imports every `*.pdf` found under `tests/fixtures/pdfs/`
when files are present. PDF files are intentionally ignored by Git and should be
supplied locally by the developer for copyright reasons.
