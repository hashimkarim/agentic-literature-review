# AD-035 live application acceptance

## Scope and provenance

- Application: `hashimkarim/agentic-literature-review`, branch `chat-reliability`.
- Reconciled handoff `4f4be9240080c776d0b7161501e038577e9b6202` in `138c22d`;
  traceability in `0b82503`, writing review in `5779d4d`, chat queue in `eda5d2b`.
  The handoff branch is preserved. Current UI/view preferences remain intact.
- Installed client remains exact registry `@agenticdriver/sdk@0.1.0`, not the
  handoff's source archive. No dependency, auth stack or SDK source changes.
- User-selected existing local Codex sign-in, `local-codex`, `gpt-6-luna`, medium
  effort fixed by the shared host at `http://127.0.0.1:17433`. No fallback.
- Only disposable synthetic research data was submitted or accepted. The
  original four requests in [the earlier receipt](writing-live-2026-09-25.md)
  were not replayed. This receipt adds ten SDK requests: nine completed and one
  host-confirmed cancellation. No live Usagestat ingestion is claimed here.

## Native browser and application checks

Used the T3 collaborative browser against the real production web build and
application HTTP backend at `http://127.0.0.1:3886`, initially 1920x1080. The
private token was read only by the server launcher; the browser calls LitAgent,
not the SDK. Public driver/provider/workflow/thread responses were checked for
credential absence. No new browser-origin grant was needed.

1. Settings: selected the exact permitted model, enabled the driver, refreshed
   discovery, simulated offline transport, disabled while offline, reloaded,
   recovered discovery and explicitly re-enabled. The selected model persisted.
   Unknown authentication remains visibly unverified, not falsely healthy.
2. Writing: selected only `synthetic-results.txt`; the context preview showed
   one complete 270-character source, no excluded sentinel source, and two model
   calls. Generated text remained a proposal until acceptance.
3. The first fresh draft had 47 estimated words against target 50. Source review
   did NOT reject length: it rejected a conclusion presented as reported fact
   rather than inference. Acceptance remained blocked. This safeguard was not
   overridden or weakened.
4. A strictly reported-facts draft passed source review. Its deterministic TeX
   prose estimate was 40/50 words, advisory only. Evidence opened the selected
   source at line 1 and revision `381d87e88cca`. Accepted only into the disposable
   `sections/introduction.tex`, producing a history-backed revision
   `8c6b10cbb8644f232fab9ed697266c42e3735c224492cb3937758b02f04863ed`.
5. A controlled run-request HTTP 503 produced an actionable chat failure and
   explicit Retry, no fabricated answer, usage or SDK ID. Discovery recovery
   and Retry completed a real cited answer (40 ms versus 30 ms, no accuracy
   evaluation). Clicking citation 2 resolved the limitations at Markdown L11.
6. The initial interrupt attempt exposed missing Queue refresh for direct Q&A;
   it finished before a Cancel button became available. Fixed polling to track
   pending chats across scopes, and to refresh final workflow state.
7. After the fix, Queue cancellation worked without reload and restored the
   unfinished question to the composer, without saving an answer. The first
   cancellation raced host completion; both outcomes are recorded below.
8. A final check directly against port 17433 (no fault proxy) confirmed both
   local and host cancellation. The thread retained exactly two answers/four
   messages. Source Markdown remained byte-identical (SHA-256
   `9062894474eb015349aa8cb0c0c9cec56884a07bd76b3e3e52eb2f9700dcfcbf`).

Offline and error behavior used the opt-in `scripts/live-driver-proxy.ts` on
port 17434. These were injected transport faults, not claims of an actual
provider outage. The shared host and Usagestat were not stopped or modified.
The native browser disconnected during final screenshot capture after the
interaction checks; no final screenshot or 1440px visual pass is claimed.

## Run and usage reconciliation

Normalized records retain LitAgent IDs plus `payload.sdkRunId`. Step usage and
run totals have distinct `usageScope` values: do not add both. Host diagnostic
JSONL confirmed the IDs and completed token totals below. Cached input is a
reported subset, not extra input. Unknown cancelled usage is not zero.

| Application stage | SDK run ID | Host outcome | Input/output/cached tokens |
| --- | --- | --- | --- |
| `candidate_a42dbaabc8aca18f` draft | `2eceb76b-1b37-458a-af4e-d36880976ec5` | completed | 14218 / 283 / 6912 |
| Same candidate, review (unsupported inference) | `87680a99-4eca-4091-a458-b9aadacb2e2a` | completed | 14360 / 256 / 13056 |
| `candidate_621a818f99d3fb0c` draft | `531bb7f8-73a8-4f73-b7f4-6d3abb551372` | completed | 14223 / 337 / 11008 |
| Same candidate, supported review | `018397d3-c3f2-4f54-990f-82a2f12d6542` | completed | 14415 / 280 / 6912 |
| `run_2880ad0d5eb64924` Q&A draft | `a556502c-d4fe-4268-90a1-1bebf05e1784` | completed | 14028 / 102 / 13056 |
| Same question, review | `458408df-34ee-42fa-a4d2-743189970c87` | completed | 14025 / 147 / 6912 |
| `run_900af489a63d4dc9` Q&A draft (Queue bug exposed) | `20dd1c01-fd6d-4625-915b-c90bbc06b20a` | completed | 14085 / 163 / 13056 |
| Same question, review | `46260237-9d7a-4730-8425-4f7631f2641f` | completed | 14225 / 154 / 11008 |
| `run_5a98a354dfc649c9` cancellation race | `e7e9a1fc-0c47-48f7-a0c6-143c9bca8782` | completed after local cancellation | 14191 / 161 / 3840 (host only) |
| `run_2ce1d2aa4f964907` direct-host interruption | `567e1ad4-5d20-479c-9a78-87beab1e1a0d` | cancelled | unknown |

Injected pre-acceptance failure: `run_e0dad781221f4c0f`, no SDK run was created.
For the race, local cancellation was recorded at 16:36:15.040Z; host completion
was 16:36:15.744Z. The local result was discarded and its unobserved usage was
not invented. Direct-host cancellation ended at 16:37:59.895Z on the host and
16:37:59.896Z locally. Usage reconciliation into Usagestat remains SDK-owner work.

Private reproducibility artifacts remain in the disposable
`litagent-writing-live-Wg3ck5` directory: research repository, candidate history,
workflow events and `.litagent/cache/provider-runs`. No credentials, generated
manuscripts, PDFs or raw provider streams were committed.

## Verification and limits

- `bun run typecheck`: pass.
- `bun run test`: 211 passed, two opt-in native compiler tests skipped, 24 files.
  One concurrent run hit a five-second HTTP fixture timeout; the unchanged
  rerun and subsequent final suite passed. Earlier concurrent fake-CLI discovery
  flakes also passed on rerun. These are not live-provider failures.
- `bun run build`: pass; final UI-only `bun run --filter @litagent/web build`
  also passed. Existing large-chunk warning remains.
- Installed-SDK fixtures cover scoped refresh, active-run preservation, offline
  disable, model restrictions, source/revision validation and normalized IDs.
- Native live interactions above cover Settings, writing evidence/acceptance,
  chat source navigation, error/retry, Queue refresh and cancellation.

AD-035 application completion criteria pass for this selected route. This is
not broad model-quality certification, packaged-desktop certification, calibrated
evidence confidence, or proof of complete Usagestat billing ingestion.

## Post-run Usagestat reconciliation

The SDK owner supplied an independently reviewed reconciliation receipt at
`/home/hashim/.local/state/agenticdriver/live-checks/20260925/usagestat/reconciliation.json`,
checked at `2026-09-25T16:42:26.959Z`. Inspection confirms all fourteen
`literature-live-validation` records (the original four plus these ten) were
accepted by the existing Usagestat backend at `http://127.0.0.1:17436`, source
`80df24132db891b80587c63f07a3016606ccb5d3`. Every literature receipt reports full
original-record readback and duplicate-submission verification.

This is post-run reconciliation of original host records, with no replayed model
requests, not automatic first-hop ingestion into the older daemon. The
host-completed/local-cancelled race remains completed in the host record;
unknown cancelled usage remains unknown. This addendum supersedes the pending
reconciliation status above, not its distinct execution outcomes or packaging
and model-quality limitations. No unrelated application receipts are published.

## Native tool-control qualification hold

The SDK owner subsequently reopened AD-021 after inspecting Responses Lite
`additional_tools` input items: native exec, clock and collaboration tools were
still advertised despite earlier top-level `body.tools` checks. The completed
text/cancellation/usage evidence above remains valid for those observed outcomes,
but does not prove native tool isolation or certify the effective native catalog.
New local Codex live requests are on hold until the SDK owner supplies a cleared
host revision. `tools:false` denotes absence of the SDK application-tool bridge,
not proof that the native CLI advertises no tools. No further live run was in
flight or started by this application thread after the hold.
